{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:best-practices", ":automergePatch", ":automergeLinters"],
  "customManagers": [
    {
      "description": "Track the Spectra frontend OCI image pinned in the Rust source.",
      "customType": "regex",
      "datasourceTemplate": "docker",
      "depNameTemplate": "ghcr.io/stargrid-systems/spectra",
      "managerFilePatterns": ["/^aperture-http/src/spectra/config\\.rs$/"],
      "matchStrings": ["ghcr\\.io/stargrid-systems/spectra:(?<currentValue>[^\"]+)"]
    }
  ],
  "packageRules": [
    {
      "description": "Always open a PR for Spectra image bumps and never pin to a digest. The runtime only resolves tag references.",
      "matchDepNames": ["ghcr.io/stargrid-systems/spectra"],
      "automerge": false,
      "pinDigests": false
    },
    {
      "description": "Do not auto-merge Rust toolchain bumps. The nightly channel moves almost daily, so auto-merge would churn the toolchain constantly.",
      "matchFileNames": ["rust-toolchain.toml"],
      "automerge": false
    }
  ]
}
